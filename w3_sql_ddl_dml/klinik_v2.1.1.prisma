datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model faskes {
  id     BigInt @id @default(autoincrement())
  nama   String
  alamat String
  email  String
  nik    String
}
